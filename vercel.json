cat > vercel.json <<'JSON'
{
  "version": 2,
  "builds": [
    { "src": "api/index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "^/(tasks(?:/.*)?|api(?:/.*)?)$", "dest": "/api/index.js" },
    { "src": "^/(.*)$", "dest": "/public/$1" }
  ]
}
JSON
